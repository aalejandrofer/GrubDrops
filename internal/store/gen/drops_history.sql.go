// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: drops_history.sql

package gen

import (
	"context"
)

const listRecentClaims = `-- name: ListRecentClaims :many
SELECT
  c.id           AS claim_id,
  c.account_id   AS account_id,
  a.display_name AS account_name,
  c.benefit_id   AS benefit_id,
  b.name         AS benefit_name,
  b.campaign_id  AS campaign_id,
  camp.name      AS campaign_name,
  camp.game      AS game,
  camp.platform  AS platform,
  c.claimed_at   AS claimed_at
FROM claims c
JOIN benefits b ON b.id = c.benefit_id
JOIN campaigns camp ON camp.id = b.campaign_id
JOIN accounts a ON a.id = c.account_id
ORDER BY c.claimed_at DESC
LIMIT ?
`

type ListRecentClaimsRow struct {
	ClaimID      string `json:"claim_id"`
	AccountID    string `json:"account_id"`
	AccountName  string `json:"account_name"`
	BenefitID    string `json:"benefit_id"`
	BenefitName  string `json:"benefit_name"`
	CampaignID   string `json:"campaign_id"`
	CampaignName string `json:"campaign_name"`
	Game         string `json:"game"`
	Platform     string `json:"platform"`
	ClaimedAt    int64  `json:"claimed_at"`
}

func (q *Queries) ListRecentClaims(ctx context.Context, limit int64) ([]ListRecentClaimsRow, error) {
	rows, err := q.db.QueryContext(ctx, listRecentClaims, limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListRecentClaimsRow
	for rows.Next() {
		var i ListRecentClaimsRow
		if err := rows.Scan(
			&i.ClaimID,
			&i.AccountID,
			&i.AccountName,
			&i.BenefitID,
			&i.BenefitName,
			&i.CampaignID,
			&i.CampaignName,
			&i.Game,
			&i.Platform,
			&i.ClaimedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
